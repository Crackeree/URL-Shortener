
DROP TABLE IF EXISTS _0;
CREATE TABLE _0(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code_0 UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code_0 ON _0(CODE);

DROP TABLE IF EXISTS _1;
CREATE TABLE _1(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code_1 UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code_1 ON _1(CODE);


DROP TABLE IF EXISTS _2;
CREATE TABLE _2(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code_2 UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code_2 ON _2(CODE);

DROP TABLE IF EXISTS _3;
CREATE TABLE _3(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code_3 UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code_3 ON _3(CODE);


DROP TABLE IF EXISTS _4;
CREATE TABLE _4(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code_4 UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code_4 ON _4(CODE);



DROP TABLE IF EXISTS _5;
CREATE TABLE _5(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code_5 UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code_5 ON _5(CODE);


DROP TABLE IF EXISTS _6;
CREATE TABLE _6(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code_6 UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code_6 ON _6(CODE);


DROP TABLE IF EXISTS _7;
CREATE TABLE _7(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code_7 UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code_7 ON _7(CODE);

DROP TABLE IF EXISTS _8;
CREATE TABLE _8(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code_8 UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code_8 ON _8(CODE);


DROP TABLE IF EXISTS _9;
CREATE TABLE _9(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code_9 UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code_9 ON _9(CODE);

DROP TABLE IF EXISTS _a;
CREATE TABLE _a(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code_a UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code_a ON _a(CODE);


DROP TABLE IF EXISTS _b;
CREATE TABLE _b(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code_b UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code_b ON _b(CODE);


DROP TABLE IF EXISTS _c;
CREATE TABLE _c(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code_c UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code_c ON _c(CODE);


DROP TABLE IF EXISTS _d;
CREATE TABLE _d(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code_d UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code_d ON _d(CODE);


DROP TABLE IF EXISTS _e;
CREATE TABLE _e(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code_e UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code_e ON _e(CODE);


DROP TABLE IF EXISTS _f;
CREATE TABLE _f(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code_f UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code_f ON _f(CODE);


DROP TABLE IF EXISTS _g;
CREATE TABLE _g(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code_g UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code_g ON _g(CODE);


DROP TABLE IF EXISTS _h;
CREATE TABLE _h(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code_h UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code_h ON _h(CODE);


DROP TABLE IF EXISTS _i;
CREATE TABLE _i(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code_i UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code_i ON _i(CODE);

DROP TABLE IF EXISTS _j;
CREATE TABLE _j(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code_j UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code_j ON _j(CODE);

DROP TABLE IF EXISTS _k;
CREATE TABLE _k(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code_k UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code_k ON _k(CODE);

DROP TABLE IF EXISTS _l;
CREATE TABLE _l(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code_l UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code_l ON _l(CODE);

DROP TABLE IF EXISTS _m;
CREATE TABLE _m(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code_m UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code_m ON _m(CODE);

DROP TABLE IF EXISTS _n;
CREATE TABLE _n(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code_n UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code_n ON _n(CODE);

DROP TABLE IF EXISTS _o;
CREATE TABLE _o(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code_o UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code_o ON _o(CODE);

DROP TABLE IF EXISTS _p;
CREATE TABLE _p(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code_p UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code_p ON _p(CODE);

DROP TABLE IF EXISTS _q;
CREATE TABLE _q(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code_q UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code_q ON _q(CODE);

DROP TABLE IF EXISTS _r;
CREATE TABLE _r(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code_r UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code_r ON _r(CODE);

DROP TABLE IF EXISTS _s;
CREATE TABLE _s(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code_s UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code_s ON _s(CODE);

DROP TABLE IF EXISTS _t;
CREATE TABLE _t(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code_t UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code_t ON _t(CODE);

DROP TABLE IF EXISTS _u;
CREATE TABLE _u(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code_u UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code_u ON _u(CODE);

DROP TABLE IF EXISTS _v;
CREATE TABLE _v(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code_v UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code_v ON _v(CODE);

DROP TABLE IF EXISTS _w;
CREATE TABLE _w(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code_w UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code_w ON _w(CODE);

DROP TABLE IF EXISTS _x;
CREATE TABLE _x(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code_x UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code_x ON _x(CODE);

DROP TABLE IF EXISTS _y;
CREATE TABLE _y(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code_y UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code_y ON _y(CODE);

DROP TABLE IF EXISTS _z;
CREATE TABLE _z(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code_z UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code_z ON _z(CODE);


DROP TABLE IF EXISTS __a;
CREATE TABLE __a(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                 CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code__a UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code__a ON __a(CODE);


DROP TABLE IF EXISTS __b;
CREATE TABLE __b(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                 CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code__b UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code__b ON __b(CODE);


DROP TABLE IF EXISTS __c;
CREATE TABLE __c(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                 CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code__c UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code__c ON __c(CODE);


DROP TABLE IF EXISTS __d;
CREATE TABLE __d(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                 CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code__d UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code__d ON __d(CODE);


DROP TABLE IF EXISTS __e;
CREATE TABLE __e(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                 CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code__e UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code__e ON __e(CODE);


DROP TABLE IF EXISTS __f;
CREATE TABLE __f(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                 CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code__f UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code__f ON __f(CODE);


DROP TABLE IF EXISTS __g;
CREATE TABLE __g(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                 CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code__g UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code__g ON __g(CODE);


DROP TABLE IF EXISTS __h;
CREATE TABLE __h(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                 CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code__h UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code__h ON __h(CODE);


DROP TABLE IF EXISTS __i;
CREATE TABLE __i(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                 CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code__i UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code__i ON __i(CODE);

DROP TABLE IF EXISTS __j;
CREATE TABLE __j(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                 CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code__j UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code__j ON __j(CODE);

DROP TABLE IF EXISTS __k;
CREATE TABLE __k(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                 CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code__k UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code__k ON __k(CODE);

DROP TABLE IF EXISTS __l;
CREATE TABLE __l(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                 CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code__l UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code__l ON __l(CODE);

DROP TABLE IF EXISTS __m;
CREATE TABLE __m(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                 CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code__m UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code__m ON __m(CODE);

DROP TABLE IF EXISTS __n;
CREATE TABLE __n(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                 CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code__n UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code__n ON __n(CODE);

DROP TABLE IF EXISTS __o;
CREATE TABLE __o(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                 CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code__o UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code__o ON __o(CODE);

DROP TABLE IF EXISTS __p;
CREATE TABLE __p(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                 CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code__p UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code__p ON __p(CODE);

DROP TABLE IF EXISTS __q;
CREATE TABLE __q(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                 CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code__q UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code__q ON __q(CODE);

DROP TABLE IF EXISTS __r;
CREATE TABLE __r(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                 CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code__r UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code__r ON __r(CODE);

DROP TABLE IF EXISTS __s;
CREATE TABLE __s(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                 CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code__s UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code__s ON __s(CODE);

DROP TABLE IF EXISTS __t;
CREATE TABLE __t(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                 CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code__t UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code__t ON __t(CODE);

DROP TABLE IF EXISTS __u;
CREATE TABLE __u(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                 CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code__u UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code__u ON __u(CODE);

DROP TABLE IF EXISTS __v;
CREATE TABLE __v(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                 CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code__v UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code__v ON __v(CODE);

DROP TABLE IF EXISTS __w;
CREATE TABLE __w(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                 CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code__w UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code__w ON __w(CODE);

DROP TABLE IF EXISTS __x;
CREATE TABLE __x(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                 CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code__x UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code__x ON __x(CODE);

DROP TABLE IF EXISTS __y;
CREATE TABLE __y(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                 CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code__y UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code__y ON __y(CODE);

DROP TABLE IF EXISTS __z;
CREATE TABLE __z(ID SERIAL PRIMARY KEY,
                 TABLE_KEY VARCHAR(1) NOT NULL,
                 CODE VARCHAR(2) NOT NULL,
                 CONSTRAINT unique_code__z UNIQUE(CODE),
                 URL_IN VARCHAR(9) NOT NULL,
                 URL VARCHAR(200) NOT NULL
);

CREATE INDEX idx_code__z ON __z(CODE);

